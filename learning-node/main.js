// const { add, subtract } = require("./utils.js/index");
const { add, subtract } = require("./utils.js"); // require by default search for index

console.log(add(2, 3));
console.log(subtract(4, 3));
